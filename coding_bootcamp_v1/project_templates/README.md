# Project Templates

These folders document the project types supported by the v7 generator. The current generator writes the files directly from C++ so it can work without a future template engine. Later versions can replace the direct writer with these editable templates.
